name: CI

on:
  push:
    branches: [main]

permissions:
  contents: read
  id-token: write

jobs:
  pipeline:
    uses: Mooroon5-CNP/ci-templates/.github/workflows/pipeline.yml@main
    with:
      app_name: "cnp-portal"
      app_port: "3000"
      config_repo_url: "https://github.com/Mooroon5-CNP/config-repo"
      target_cloud: "gcp"
    secrets:
      CONFIG_REPO_TOKEN: ${{ secrets.CONFIG_REPO_TOKEN }}

  update-cloudrun-claim:
    needs: pipeline
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: Mooroon5-CNP/config-repo
          token: ${{ secrets.CONFIG_REPO_TOKEN }}
      - name: Update Cloud Run image tag
        run: |
          sed -i "s|image: europe-west9-docker.pkg.dev/cnp-terraform-500015/cnp-registry/cnp-portal:.*|image: europe-west9-docker.pkg.dev/cnp-terraform-500015/cnp-registry/cnp-portal:${{ github.sha }}|" \
            apps/cnp-portal/crossplane/cloudrun-claim.yaml
          git config user.email "ci-bot@cnp"
          git config user.name "CI Bot"
          git add apps/cnp-portal/crossplane/cloudrun-claim.yaml
          git diff --staged --quiet && exit 0
          git commit -m "ci(cnp-portal): cloud run → ${{ github.sha }}"
          for i in 1 2 3; do
            git pull --rebase && git push && break || sleep 5
          done
