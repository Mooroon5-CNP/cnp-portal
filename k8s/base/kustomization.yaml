apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cnp-portal

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: "cnp-portal"
  app.kubernetes.io/owner: "team-cnp"
  cost-center: "projet-epita"
